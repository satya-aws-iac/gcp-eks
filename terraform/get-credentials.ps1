<#
get-credentials.ps1 removed — GKE credential step was intentionally removed.
If you need a helper again, recreate this file or run gcloud manually with:
  gcloud container clusters get-credentials <CLUSTER> --zone <ZONE> --project <PROJECT>
#>

