name: Build and Deploy

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: satya-k8-poc
  REGION: us-central1
  REPOSITORY: satya-gcr
  IMAGE_NAME: aws-cloud-projects-site

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    ...existing code...
    env:
      GKE_CLUSTER_NAME: ${{ secrets.GKE_CLUSTER_NAME }}
      GKE_LOCATION: ${{ env.REGION }}
      # Optional: set DOCKERFILE_PATH if your Dockerfile is not at the repo root
      DOCKERFILE_PATH: Dockerfile

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      ...existing code...

      - name: Build and Push Docker image
        run: |
          if [ ! -f "${DOCKERFILE_PATH}" ]; then
            echo "Dockerfile not found at '${DOCKERFILE_PATH}'"
            echo "Repository files:"
            ls -R
            exit 1
          fi

          IMAGE_TAG="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          IMAGE_LATEST="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE_NAME }}:latest"

          docker build -f "${DOCKERFILE_PATH}" -t $IMAGE_TAG -t $IMAGE_LATEST .
          docker push $IMAGE_TAG
          docker push $IMAGE_LATEST

          echo "✅ Image pushed successfully!"
          echo "📦 Image: $IMAGE_LATEST"
      ...existing code...

      - name: Get GKE credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.GKE_CLUSTER_NAME }}
          location: ${{ env.GKE_LOCATION }}
      ...existing code...